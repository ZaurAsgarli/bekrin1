# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1

# Database — PostgreSQL only (no SQLite fallback). Port 5433 if not default.
# Option 1: DATABASE_URL (recommended)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:bekrin123@localhost:5433/postgres

# Option 2: If DATABASE_URL is unset, use these (PORT required if Postgres not on 5432)
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=bekrin123
# DB_HOST=localhost
# DB_PORT=5433

# DB connection pooling (0=disabled, 60 recommended for production)
# DB_CONN_MAX_AGE=60

# CORS & CSRF (frontend localhost:3000)
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

# Credential encryption (required for bulk import credential registry)
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# CREDENTIALS_ENCRYPTION_KEY=

# JWT Settings
JWT_ACCESS_LIFETIME_MINUTES=60
JWT_REFRESH_LIFETIME_DAYS=7

# Seed Data (for development)
SEED_TEACHER_EMAIL=teacher@bekrinschool.az
SEED_TEACHER_PASSWORD=teacher123
SEED_STUDENT_PASSWORD=student123
SEED_PARENT_PASSWORD=parent123
